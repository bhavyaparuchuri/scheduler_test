#include "gtest/gtest.h"
#include <stdexcept>
#include "../inc/DB/DB_Control.h"

#define FALSE 0
#define TRUE 1

typedef unsigned int    UWORD16;

UWORD16 DB_ControlRead(e_DB_CONTROL Id)
{
	return (DB_Control[Id]);	
}

TEST(Database,DB_ControlRead){
	  EXPECT_EQ(TRUE,DB_ControlRead(MAINTENANCE_MODE_U16));

	  EXPECT_EQ(TRUE,DB_ControlRead(CIRCUIT_CHECK_MODE_U16));
}

